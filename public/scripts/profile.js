document.querySelector("video").playbackRate = 0.7;





/* Notes
function updateEmail() {
    const emailInput = document.querySelector('#profile-properties input[type="email"]');
    const email = emailInput.value;
    if (email) {
        console.log(`Email updated to: ${email}`);
        // TODO
    } else {
        alert('Please enter a valid email.');
    }
}

function updatePassword() {
    const passwordInput = document.querySelector('#profile-properties input[type="password"]');
    const password = passwordInput.value;
    if (password) {
        console.log('Password updated.');
        // TODO
    } else {
        alert('Please enter a valid password.');
    }
}

function updateProfilePicture() {
    const fileInput = document.querySelector('#profile-properties input[type="file"]');
    const file = fileInput.files[0];
    if (file) {
        console.log(`Profile picture updated: ${file.name}`);
        // TODO
    } else {
        alert('Please select a valid image file.');
    }
}
*/